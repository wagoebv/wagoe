= Wagoe Framework Roadmap
:toc:
:toclevels: 3
:icons: font

Wagoe is a module-centric Clojure framework implementing Functional Core / Imperative Shell. This roadmap tracks progress toward 1.0 and beyond. Updated April 2026.

== Phase 0 — Alpha icon:check-circle[] *COMPLETE*
*Goal: Make Wagoe usable for early adopters with minimal friction.*

=== Delivered
- [x] Core APIs frozen (FC/IS, Interceptors, Normalized Routing)
- [x] All modules use the normalized routing system
- [x] Scaffolder generates normalized routes, interceptor hooks, CRUD stubs
- [x] Beginner documentation: Getting Started, Your First Module, FC/IS guide
- [x] Public release: GitHub open, Clojars deployment (`bb deploy --all`)
- [x] 22 independently publishable libraries under `libs/` (+ `libs/tools` for dev tooling)
- [x] Example application: `examples/ecommerce-api/`

'''

== Phase 1 — Beta icon:check-circle[] *COMPLETE*
*Goal: Make Wagoe comfortable and delightful to build real-world applications.*

=== Delivered
- [x] Full CRUD Web UI for User module (create, edit, delete, MFA, sessions) via HTMX + Hiccup
- [x] Auto-CRUD Admin interface (`libs/admin`) with entity discovery, forms, filters, bulk actions
- [x] Example interceptors: authentication, audit logging, rate limiting
- [x] Scaffolder: NL mode (`bb scaffold ai`), interactive wizard, field/endpoint/adapter generation
- [x] Payments library (`libs/payments`): Mollie, Stripe, Mock PSP adapters
- [x] Multi-tenancy (`libs/tenant`): schema-per-tenant, provisioning, lifecycle states
- [x] Distributed caching (`libs/cache`): Redis + in-memory adapters
- [x] Background jobs (`libs/jobs`): worker pool, retry logic, dead letter queue
- [x] Email (`libs/email`): domain model, SMTP adapter, jobs integration
- [x] Real-time messaging (`libs/realtime`): WebSocket + Ring WebSocket handlers, pub/sub
- [x] Internationalisation (`libs/i18n`): EDN catalogues, locale resolution, BB tooling
- [x] Shared UI styles (`libs/ui-style`): Tailwind/DaisyUI bundles
- [x] Antora documentation site (`docs/`)
- [x] CI/CD: GitHub Actions publish workflow, `bb doctor --ci`
- [x] External services adapters (`libs/external`): Twilio SMS/WhatsApp, SMTP, IMAP

'''

== Phase 2 — v1.0 (Stability & Production Readiness) icon:check-circle[] *COMPLETE*
*Goal: Make Wagoe fully reliable and attractive for company-level adoption.*

=== Objectives
- Build trust through stability and polish
- Provide config & integration tooling to reduce setup friction
- Improve operational and security guidance

=== Delivered
- [x] `libs/tools`: scaffolding, AI, i18n, deploy, dev utilities (dev tooling, not published to Clojars)
- [x] Config Doctor (`bb doctor`): validates env refs, providers, JWT, admin parity, wiring
- [x] Config Setup Wizard (`bb setup`): interactive + AI-powered config generation
- [x] Module Integration (`bb scaffold integrate`): wires scaffolded modules into deps/tests/wiring
- [x] Admin Entity Generator (`bb ai admin-entity`): AI-powered EDN config generation
- [x] Datadog metrics provider (`libs/observability`)
- [x] Performance optimisations: window functions, batch inserts, Redis SCAN, caching, gzip
- [x] Tenant admin dashboard
- [x] Health check endpoints with dependency checks (`/health/ready` checks DB + cache connectivity)
- [x] Security checklist (`dev-docs/security-checklist.adoc`): OWASP Top 10 alignment, headers, secrets, auth
- [x] Deployment patterns: systemd, nginx, Fly.io, Render reference configs (`resources/deploy/`)
- [x] Deployment patterns guide (`docs/modules/guides/pages/deployment-patterns.adoc`)
- [x] Database migration best practices guide (`docs/modules/guides/pages/migrations.adoc`)
- [x] Guided 10-minute quickstart (`docs/modules/getting-started/pages/quickstart.adoc`): `wagoe new` to running app
- [x] CRUD benchmarks (`dev/wagoe/bench/core.clj`): Criterium benchmarks via `clojure -M:bench`
- [x] Scaling & tuning guide (`dev-docs/scaling-guide.adoc`): JVM, HikariCP, Redis, horizontal scaling
- [x] Admin UI "Refined Editorial" redesign: DM Sans + JetBrains Mono, layered shadows, HTMX loading bar, toast system, skeleton screens, styled delete modal
- [x] Full admin UI internationalisation: entity configs, filter operators, flash messages, inline edit, breadcrumbs, sidebar tooltips all use `[:t :key]` markers (EN + NL catalogues)
- [x] i18n middleware render-time locale resolution: `resolve-t-fn` evaluates user locale after auth middleware on the route-level pipeline

'''

== Phase 3 — Ecosystem Expansion (Post-1.0)
*Goal: Build a vibrant Wagoe ecosystem and optional advanced capabilities.*

=== Delivered (ahead of schedule)
- [x] Workflow engine (`libs/workflow`): state machines with guards, lifecycle hooks, auto-transitions, audit trails
- [x] Full-text search (`libs/search`): PostgreSQL FTS / H2 LIKE fallback, weighted fields, filter support, admin UI
- [x] Report generation (`libs/reports`): `defreport` macro, PDF/Excel/DOCX export
- [x] Calendar / events (`libs/calendar`): `defevent` macro, RRULE recurrence, iCal, conflict detection, Hiccup UI
- [x] Geocoding (`libs/geo`): OSM/Google/Mapbox providers, DB cache, Haversine distance
- [x] AI tooling (`libs/ai`): Ollama/Anthropic/OpenAI, NL scaffolding, error explainer, test generator, SQL copilot, docs wizard

=== Remaining
- [ ] Push notifications library (`wagoe-push`): APNs, FCM adapters
- [ ] Audience / segmentation library (`wagoe-audience`): targeted messaging, cohorts
- [ ] Forms library (`wagoe-forms`): dynamic form definitions, validation, rendering
- [ ] Realtime multi-server scaling: Redis-backed registry and pub/sub
- [ ] Datomic / XTDB backend integration
- [ ] Wagoe SaaS scaffold preset (login, storage, admin, payments) via `wagoe new`
- [ ] Application templates: API-only, admin dashboard, SaaS boilerplate
- [ ] Extension / plugin marketplace

'''

== Roadmap Summary

[cols="1,2,1,1"]
|===
|Phase |Focus |Status |Libraries

|*Alpha*
|Quick release, scaffolder, docs, Clojars
|icon:check-circle[] Complete
|7 initial

|*Beta*
|Developer productivity, admin UI, domain modules
|icon:check-circle[] Complete
|22 libraries

|*v1.0*
|Config tooling, stability, production readiness
|icon:check-circle[] Complete
|22 + libs/tools

|*Ecosystem*
|Advanced capabilities, templates, integrations
|Partially delivered (6 of 9 libraries done)
|workflow, search, reports, calendar, geo, ai
|===

=== Test Health

As of April 2026: **3044 tests, 15742 assertions, 0 failures** across all 22 libraries (`clojure -M:test:db/h2`). CI runs per-library jobs plus a whole-suite gate, lint, docs-lint, and AGENTS link checks on every push.

== Strategic Notes

=== 1. Focus on onboarding, not architectural depth
Wagoe already has deep documentation. The quickstart (`docs/modules/getting-started/pages/quickstart.adoc`) provides a narrative, 10-minute experience for beginners, from `wagoe new` to a running app.

=== 2. Use the Scaffolder as your flagship feature
Wagoe's scaffolder (`bb scaffold`) is a key differentiator — it should define the developer workflow from day one. With NL scaffolding (`bb scaffold ai`) and module integration (`bb scaffold integrate`), the end-to-end module creation flow is now nearly automated.

=== 3. Developer tooling reduces friction
Config Doctor (`bb doctor`), Setup Wizard (`bb setup`), and the AI tools (`bb ai`) address the biggest friction points for new developers: configuration, debugging, and boilerplate.

=== 4. Publish early, iterate publicly
The framework is already more mature than most early-stage OSS releases. Early feedback accelerates correctness.

=== 5. First-class internationalisation
With the admin UI fully translatable via `[:t :key]` markers in entity configs and catalogues shipped for English and Dutch, Wagoe is ready for non-English-speaking markets out of the box. New modules inherit i18n support automatically.

'''

== Next Review
This roadmap is intended as a living document. Revisit every 2–3 weeks to adjust scope and priorities as adoption grows.
