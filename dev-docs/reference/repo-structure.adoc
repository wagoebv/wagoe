= Repository Structure
:toc: left
:toclevels: 3

[[overview]]
== Overview

Wagoe uses a **monorepo with modular libraries** architecture. After the library split (v2.2.0), all domain and infrastructure code lives in the `libs/` directory, with each library owning its complete functionality stack.

This document is the **canonical reference** for repository structure and paths after the library split.

[[library-overview]]
== Library Overview

[cols="1,2,3"]
|===
|Library |Responsibility |Key Components

|`libs/core`
|Foundation utilities, validation, interceptors
|Case conversion, type coercion, shared schemas, interceptor patterns

|`libs/observability`
|Logging, metrics, error reporting
|Logger protocols, metrics registry, Sentry/Datadog adapters

|`libs/platform`
|HTTP, database, CLI infrastructure
|Ring/Reitit routing, database context, CLI entry points

|`libs/user`
|Authentication, authorization, MFA
|User service, session management, JWT tokens, web UI

|`libs/admin`
|Auto-CRUD admin interface
|Entity management, filter builder, bulk actions, Hiccup components

|`libs/storage`
|File storage (local and S3)
|Storage protocols, local adapter, S3 adapter

|`libs/scaffolder`
|Module code generator
|Templates, code generation, CLI interface

|`libs/cache`
|Distributed caching
|Cache protocols, Redis adapter, in-memory adapter, TTL, atomic ops

|`libs/jobs`
|Background job processing
|Job queue, worker pool, retry logic, dead letter queue

|`libs/email`
|Application-level email sending
|Email domain model, validation, preparation, SMTP adapter (delegates transport to `libs/external`), jobs integration

|`libs/realtime`
|WebSocket / SSE messaging
|WebSocket protocols, pub/sub, JWT auth, message routing

|`libs/tenant`
|Multi-tenancy
|Schema-per-tenant isolation, provisioning, lifecycle states

|`libs/external`
|External service adapters: raw transport and third-party APIs
|SMTP transport (javax.mail), IMAP mailbox, Twilio SMS / WhatsApp

|`libs/payments`
|Payment Service Provider abstraction
|PSP protocols, Mollie/Stripe/Mock adapters, checkout flow, webhook verification

|`libs/workflow`
|Workflow orchestration engine
|State machines, step execution, status tracking

|`libs/search`
|Full-text search
|Search protocols, indexing, query building

|`libs/reports`
|Report generation (`defreport` macro)
|PDF/CSV export, scheduling, report registry

|`libs/calendar`
|Calendar and event management (`defevent` macro)
|RRULE recurrence, iCal export, conflict detection, Hiccup UI

|`libs/geo`
|Multi-provider geocoding
|OSM/Google/Mapbox providers, DB-backed cache, Haversine distance

|`libs/ai`
|Multi-provider AI tooling
|Ollama/Anthropic/OpenAI providers, NL scaffolding, error explainer, test generator, SQL copilot

|`libs/i18n`
|Internationalisation
|EDN translation catalogues, locale resolution, marker-based i18n

|`libs/ui-style`
|Shared UI style bundles
|CSS/JS assets, design tokens, Tailwind/DaisyUI builds (`:base`, `:pilot`, `:admin-pilot`)

|`libs/tools/`
|Babashka developer tooling (not published to Clojars)
|Scaffolding, AI CLI, config doctor, setup wizard, deploy, i18n tools
|===

[[canonical-paths]]
== Canonical Path Structure

Each library follows the **Functional Core / Imperative Shell** pattern:

[source]
----
libs/{library}/
├── deps.edn                    # Library-specific dependencies
├── src/
│   └── wagoe/
│       └── {library}/
│           ├── core/           # Pure business logic (no I/O)
│           │   ├── {domain}.clj
│           │   └── ui.clj      # Hiccup UI components (if applicable)
│           ├── shell/          # I/O, validation, side effects
│           │   ├── service.clj
│           │   ├── persistence.clj
│           │   ├── http.clj
│           │   └── web_handlers.clj
│           ├── ports.clj       # Protocol definitions
│           └── schema.clj      # Malli validation schemas
└── test/
    └── wagoe/
        └── {library}/
            ├── core/           # Unit tests (pure functions)
            └── shell/          # Integration/contract tests
----

[[shared-namespace]]
=== Shared Namespace

Cross-cutting utilities live under `wagoe.shared` within the appropriate library:

[source]
----
libs/core/src/wagoe/shared/
├── core/
│   └── utils/
│       ├── case_conversion.clj   # kebab-case <-> snake_case
│       └── type_conversion.clj   # Instant, BigDecimal, etc.
└── shell/
    └── interceptors.clj          # Observability interceptors
----

[[example-library-structure]]
=== Example: User Library

[source]
----
libs/user/
├── deps.edn
├── src/
│   └── wagoe/
│       └── user/
│           ├── core/
│           │   ├── user.clj          # User entity logic
│           │   ├── session.clj       # Session management
│           │   ├── mfa.clj           # MFA logic
│           │   ├── audit.clj         # Audit logging
│           │   └── ui.clj            # Hiccup components
│           ├── shell/
│           │   ├── service.clj       # UserService implementation
│           │   ├── persistence.clj   # UserRepository implementation
│           │   ├── http.clj          # REST API routes
│           │   └── web_handlers.clj  # Web UI handlers
│           ├── ports.clj             # IUserService, IUserRepository
│           └── schema.clj            # User, Session schemas
└── test/
    └── wagoe/
        └── user/
            ├── core/
            │   ├── user_test.clj
            │   └── user_validation_snapshot_test.clj
            └── shell/
                ├── service_test.clj
                └── persistence_test.clj
----

[[root-structure]]
== Root Repository Structure

[source]
----
wagoe/
├── libs/                        # All 21 library modules
│   ├── core/                    # Foundation utilities, validation
│   ├── observability/           # Logging, metrics, error reporting
│   ├── platform/                # HTTP, database, CLI infrastructure
│   ├── user/                    # Authentication, authorization, MFA
│   ├── admin/                   # Auto-CRUD admin interface
│   ├── storage/                 # File storage (local + S3)
│   ├── scaffolder/              # Module code generator
│   ├── cache/                   # Distributed caching (Redis / in-memory)
│   ├── jobs/                    # Background job processing
│   ├── email/                   # Application-level email
│   ├── realtime/                # WebSocket / SSE messaging
│   ├── tenant/                  # Multi-tenancy (schema-per-tenant)
│   ├── external/                # External adapters (SMTP, IMAP, Twilio)
│   ├── payments/                # PSP abstraction (Mollie, Stripe, Mock)
│   ├── workflow/                # Workflow orchestration
│   ├── search/                  # Full-text search
│   ├── reports/                 # Report generation (defreport)
│   ├── calendar/                # Calendar / events (defevent)
│   ├── geo/                     # Multi-provider geocoding
│   ├── ai/                      # Multi-provider AI tooling
│   ├── i18n/                    # Internationalisation
│   └── ui-style/                # Shared UI style bundles
│   └── tools/                   # Babashka developer tooling (bb tasks, not published to Clojars)
│       ├── src/wagoe/tools/  # scaffold, ai, doctor, setup, deploy, etc.
│       └── test/wagoe/tools/ # Unit tests (bb test:tools)
├── resources/
│   ├── conf/                    # Environment-based Aero configuration
│   │   ├── dev/
│   │   │   ├── config.edn
│   │   │   └── admin/           # Admin entity configs
│   │   ├── test/
│   │   ├── prod/
│   │   └── acc/
│   ├── public/                  # Static assets (css, js)
│   └── migrations/              # Database migrations
├── docs/                        # Antora documentation site
│   └── modules/                 # getting-started, architecture, guides, libraries
├── dev-docs/                    # Internal developer documentation
│   ├── adr/                     # Architecture Decision Records (ADR-001..020)
│   └── reference/               # Commands, repo structure, publishing
├── scripts/                     # Tooling scripts
│   └── docs_lint.clj
├── dev/                         # Development utilities (REPL, test reporter)
├── build/                       # Build output (gitignored)
├── deps.edn                     # Root deps with aliases
├── bb.edn                       # Babashka task definitions
├── tests.edn                    # Kaocha test configuration
├── build.clj                    # Build tasks (uberjar)
├── AGENTS.md                    # AI agent quick reference
├── CLAUDE.md                    # Claude Code instructions
├── CHANGELOG.md                 # Release notes
└── BUILD.md                     # Uberjar build guide
----

[[dependency-rules]]
== Dependency Rules

Libraries have explicit dependency relationships:

[source]
----
;; Foundation
platform --> core
platform --> observability

;; Infrastructure
external --> platform
cache    --> platform
jobs     --> platform
storage  --> core, platform

;; Domain modules
user     --> core, platform, observability
admin    --> core, platform, user, ui-style
tenant   --> platform, user
email    --> platform, external
realtime --> platform
payments --> (standalone, no internal deps)
workflow --> platform
search   --> platform
reports  --> platform
calendar --> platform
geo      --> platform
ai       --> platform

;; Standalone
i18n      --> (standalone, no internal deps)
ui-style  --> (standalone, no internal deps)
scaffolder --> core
----

**Rules:**

* Libraries can only depend on libraries above them in the hierarchy
* `core` has no internal dependencies (foundation layer)
* `platform` provides infrastructure that other modules consume
* Domain libraries (`user`, `admin`, `tenant`, `cache`, `jobs`, `realtime`, etc.) depend on platform
* `email` delegates raw SMTP transport to `external`; `external` sits at the platform level
* `payments`, `i18n`, and `ui-style` are standalone — no internal Wagoe deps
* `wagoe-tools` is consumed via `{:local/root "libs/tools"}` in `bb.edn`, not as a library dependency

[[configuration]]
== Configuration Structure

Configuration uses Aero with environment-based profiles:

[source]
----
resources/conf/
├── dev/
│   ├── config.edn              # Main development config
│   └── admin/
│       └── users.edn           # User entity admin config
├── test/
│   └── config.edn              # Test configuration
└── prod/
    └── config.edn              # Production configuration
----

Admin entity configs are modular - each module owns its config in `admin/{module}.edn`:

[source,clojure]
----
;; In config.edn
:wagoe/admin
{:enabled? true
 :entities #merge [#include "admin/users.edn"]
 :pagination {...}}
----

[[testing-structure]]
== Testing Structure

Tests are organized by category and library:

[cols="1,2,2"]
|===
|Category |Location |Purpose

|Unit tests
|`libs/{library}/test/wagoe/{library}/core/`
|Pure function tests, no I/O

|Integration tests
|`libs/{library}/test/wagoe/{library}/shell/`
|Service tests with mocked dependencies

|Contract tests
|`libs/{library}/test/wagoe/{library}/shell/`
|Adapter tests with real database
|===

Test metadata tags:

* `:unit` - Pure function tests
* `:integration` - Service-level tests
* `:contract` - Database contract tests

[[migration-from-old-structure]]
== Migration from Old Structure

See link:migrations/library-split.adoc[Library Split Migration Guide] for the complete mapping from pre-split paths to current paths.

[[related-documentation]]
== Related Documentation

* link:development-commands.adoc[Development Commands] - Test, lint, REPL, build commands
* link:migrations/library-split.adoc[Library Split Migration] - Old to new path mapping
* link:../architecture/overview.adoc[Architecture Overview] - FC/IS patterns and design
