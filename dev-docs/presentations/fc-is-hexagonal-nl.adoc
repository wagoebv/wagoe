= Voorbij MVC: Functional Core / Imperative Shell
:author: Thijs Creemers
:revdate: 2026-03-07
:description: Een presentatie van 60 minuten voor Django/Rails/Spring-engineers over FC/IS en hexagonale architectuur, geïmplementeerd in het Wagoe framework.
:toc: left
:toc-title: Inhoud
:toclevels: 2
:icons: font
:source-highlighter: rouge
:rouge-style: monokai
:imagesdir: images

// ─────────────────────────────────────────────────────────────────────────────
// SLIDE 1 — TITEL
// ─────────────────────────────────────────────────────────────────────────────
== Voorbij MVC: Functional Core / Imperative Shell

_Een verhaal van iemand die een "rabit hole" in dook — en op de bodem een framework bouwde._

[.notes]
--
* "Hallo allemaal."
* "De afgelopen tijd heb ik een framework gebouwd met de naam  Wagoe. Niet omdat het ecosysteem een nieuw framework nodig had, maar omdat ik dit architectuurpatroon echt wilde *begrijpen* — en de beste manier voor mij om iets te begrijpen is het zelf bouwen."
* "Vandaag wil ik delen wat ik gedurende dat proces heb geleerd. En ondanks het feit dat ik voor het schrijven van het framework clojure gebruikt heb, ben ik er van overtuigd dat de inzichten die ik opgedaan heb ook relevant kunnen zijn voor jullie Python- Java- en Ruby-code."
* Toon: nieuwsgierige collega, geen evangelist. "Hier is iets cools dat ik heb gevonden."
--

// ─────────────────────────────────────────────────────────────────────────────
// SLIDE 2 — WAAROM IK WAGOE BOUWDE
// ─────────────────────────────────────────────────────────────────────────────
== Waarom ik Wagoe?

Ik een simpele vraag:

[quote]
____
Hoe zou een framework eruit zien en functioneren als architectuurregels worden *afgedwongen worden door de taal* in plaats van door code reviews?
____

Dus bouwde ik een framework. +
13 onafhankelijk publiceerbare libraries. +
Eén eigenzinnig patroon dat door alles heen loopt. +
Véél late nachten.

Het patroon heet **Functional Core / Imperative Shell**. +
En het heeft de manier waarop ik over *elke* codebase denk veranderd — dan bedoel ik ook de code waar wij elke dag aan werken in Django-, Rails- en Springboot codebases .

[.notes]
--
* "Niet om Django of Rails niet te vervangen. maar om een architectuurvraag te beantwoorden."
* "De vraag bleek een heel elegant antwoord te hebben — en dat antwoord is wat ik je vandaag wil laten zien."
* "Spoiler: je hebt geen Clojure nodig, en ook Wagoe niet, om dit toe te passen. Ik ga het laten zien."
--

// ─────────────────────────────────────────────────────────────────────────────
// SLIDE 3 — AGENDA
// ─────────────────────────────────────────────────────────────────────────────
== Wat we behandelen

. Het probleem met hoe we normaal code schrijven — en waarom het eigenlijk niet onze schuld is
. Het ene inzicht dat alles verandert
. Hexagonale architectuur: ports, adapters en waarom de naam zo slecht is
. Wagoe: 13 modules, één patroon, eindeloze uitwisselbaarheid
. Vijf "aha"-momenten met echte code
. Is dit de moeite waard? (Eerlijk antwoord: soms wel, soms niet)

[.notes]
--
* "We bekijken echte Django- en Rails-code. Ik doe mijn best het niet te laten aanvoelen als een interventie."
* "Er komt Clojure aan bod. Ik probeer de syntaxschok tot een minimum te beperken."
--

// ─────────────────────────────────────────────────────────────────────────────
// SECTIE 1 — DE PIJN
// ─────────────────────────────────────────────────────────────────────────────
== Deel 1: De Pijn

_Dit kennen jullie. Jullie hebben het meegemaakt._

// ─────────────────────────────────────────────────────────────────────────────
// SLIDE 4 — HET "FAT model"
// ─────────────────────────────────────────────────────────────────────────────
=== Het "Vette model": Een Liefdesverhaal

Het begint altijd zo onschuldig.

[source,python]
----
# models.py, Jaar 1 — schoon, verstandig
class User(AbstractBaseUser):
    email    = models.EmailField(unique=True)
    password = models.CharField(max_length=128)
----

[source,python]
----
# models.py, Jaar 3 — "het leek een goed idee"
class User(AbstractBaseUser):

    def register(self, email, password, plan="free"):
        if not self._is_allowed_domain(email):      # bedrijfsregel
            raise ValidationError("Domein niet toegestaan")

        self.password = make_password(password)      # hashing (I/O)

        if User.objects.filter(email=email).exists(): # databasequery
            raise ValidationError("Al geregistreerd")

        send_mail("Welkom!", "...", FROM, [email])   # e-mail (netwerk-I/O)
        stripe.Customer.create(email=email)           # facturering (externe API)
        self.save()                                   # opslaan (I/O)
----

[.notes]
--
* "Jaar 1 is altijd mooi. Jaar 3 is wanneer de sollicitatiegesprekken beginnen."
* "Niemand *besloot* Stripe in het model te stoppen. Het is er gewoon... terecht gekomen. Zwaartekracht."
* Vraag het publiek: "Wie heeft een model dat meer dan 3 van deze dingen doet? Houd je hand op. ... Ja."
--

// ─────────────────────────────────────────────────────────────────────────────
// SLIDE 5 — RAILS CALLBACKS
// ─────────────────────────────────────────────────────────────────────────────
=== Rails Callbacks: Magie met Gevolgen

[source,ruby]
----
# app/models/user.rb
class User < ApplicationRecord
  before_create :hash_password
  after_create  :send_welcome_email   # vuurt... soms. in een transactie. misschien.
  after_create  :charge_stripe         # want wat kan er misgaan
  after_commit  :invalidate_cache      # op welke verbinding? goede vraag.

  def register!(email, password, plan: :free)
    raise "Domein niet toegestaan" unless allowed_domain?(email)
    raise "Al in gebruik"          if User.exists?(email: email)
    self.email = email
    self.plan  = plan
    save!
  end
end
----

[quote]
____
`after_create` — perfect voor neveneffecten die je bijna altijd wilt, behalve in tests, en mogelijk in productie op dinsdagen.
____

[.notes]
--
* "Rails is elegant en mooi. ActiveRecord is een meesterwerk... voor de eerste 18 maanden van een project."
* "Callbacks zijn de plek waar logica onzichtbaar wordt. Je roept `save!` aan en je weet niet meer wat er gebeurt."
* "VCR-cassettes, `ActionMailer::Base.deliveries`, let(:stripe_mock) — de testsuite begint op een toneelproductie te lijken."
--

// ─────────────────────────────────────────────────────────────────────────────
// SLIDE 6 — SPRING BOOT HETZELFDE VERHAAL
// ─────────────────────────────────────────────────────────────────────────────
=== Spring Boot — Hetzelfde Verhaal, Enterprise-editie

Spring Boot heeft dependency injection en een servicelaag. Probleem opgelost, toch? +
_Niet helemaal._

[source,java]
----
// UserService.java — een volstrekt normale Spring-service
@Service
@Transactional
public class UserService {

    @Autowired private UserRepository  userRepository;   // JPA — database
    @Autowired private PasswordEncoder passwordEncoder;  // bcrypt — CPU-I/O
    @Autowired private JavaMailSender  mailSender;       // SMTP — netwerk-I/O
    @Autowired private StripeClient    stripeClient;     // REST — externe API

    public User register(RegisterRequest req) {
        // Bedrijfsregel + databaseaanroep in één adem
        if (userRepository.existsByEmail(req.email()))
            throw new EmailAlreadyExistsException();

        if (!isAllowedDomain(req.email()))               // pure bedrijfsregel
            throw new DomainNotAllowedException();       // zit vast in dezelfde klasse

        String hash = passwordEncoder.encode(req.password()); // I/O
        User user   = userRepository.save(                    // I/O
                          new User(req.email(), hash));

        sendWelcomeEmail(user);                          // I/O
        stripeClient.createCustomer(user.getEmail());    // I/O

        return user;
    }
}
----

DI is prima. Maar `UserService` *kent* nog steeds bedrijfsregels ÉN *doet* I/O — in dezelfde klasse.

[.notes]
--
* "Spring-devs zeggen: 'maar we hebben interfaces en DI!' Klopt — en dat is al beter dan ActiveRecord. Maar de *service* mengt nog steeds logica en I/O."
* "De `isAllowedDomain`-regel woont in dezelfde klasse als `stripeClient.createCustomer`. Ze hebben niets met elkaar te maken."
* "`@Transactional` is een verborgen neveneffect. Je roept `register()` aan en een transactie wordt onzichtbaar geopend."
* "De service *orkestreert* én *beslist* in één adem. FC/IS zegt: dat hoort thuis in aparte namespaces ."
--

// ─────────────────────────────────────────────────────────────────────────────
// SLIDE 7 — DE TESTBELASTING
// ─────────────────────────────────────────────────────────────────────────────
=== De belasting op tests — Alle Drie de Stacks

Om *één* bedrijfsregel te testen — "is dit e-maildomein toegestaan?" — heb je nodig:

.Django
[source,python]
----
@patch("myapp.models.stripe.Customer.create")   # niet echt afschrijven
@patch("myapp.models.send_mail")                # niet echt mailen
@patch("myapp.models.make_password", return_value="hashed")
@pytest.mark.django_db
def test_domain_not_allowed(mock_hash, mock_mail, mock_stripe):
    user = User()
    with pytest.raises(ValidationError):
        user.register("alice@concurrent.com", "s3cr3t")
    mock_stripe.assert_not_called()
    mock_mail.assert_not_called()
----

.Spring Boot
[source,java]
----
@ExtendWith(MockitoExtension.class)    // in ieder geval geen volledige Spring-context...
class UserServiceTest {

    @Mock UserRepository  userRepository;  // vier mocks
    @Mock PasswordEncoder passwordEncoder;
    @Mock JavaMailSender  mailSender;
    @Mock StripeClient    stripeClient;
    @InjectMocks UserService userService;

    @Test void rejectsDomainNotAllowed() {
        when(passwordEncoder.encode(any())).thenReturn("hashed");
        when(userRepository.existsByEmail(any())).thenReturn(false);

        assertThrows(DomainNotAllowedException.class, () ->
            userService.register(new RegisterRequest("alice@concurrent.com", "s3cr3t")));

        verify(stripeClient, never()).createCustomer(any()); // paranoia
        verify(mailSender,   never()).send(any());           // nog meer paranoia
    }
}
----

Vier mocks in beide gevallen — alleen maar om een domeincheck te testen die niets met Stripe of e-mail te maken heeft.

[.notes]
--
* "De Spring-versie heeft geen `@SpringBootTest` nodig als je `@ExtendWith(MockitoExtension.class)` gebruikt — terecht. Maar vier Mockito-mocks voor een string-check is nog steeds de verkeerde abstractie."
* "Elke keer dat de service een nieuwe dependency krijgt, moet elke test in deze klasse worden bijgewerkt."
* "De mocks testen *of je eraan gedacht hebt Stripe niet aan te roepen* — niet de bedrijfsregel zelf."
* "De architectuur heeft deze testlast gecreëerd. De architectuur kan het ook oplossen."
--

// ─────────────────────────────────────────────────────────────────────────────
// SLIDE — WAAROM BLIJFT DIT GEBEUREN?
// ─────────────────────────────────────────────────────────────────────────────
=== Waarom Blijft Dit Gebeuren?

Omdat het framework *één voor de hand liggende plek* heeft om dingen neer te zetten:

[mermaid,format=svg,width=100%]
....
flowchart TD
    Browser -->|HTTP-verzoek| C[Controller / View]
    C --> M["Model 🧲"]
    M -->|SQL| DB[(Database)]
    M -->|SMTP| Email[E-mail]
    M -->|REST| Stripe[Stripe]
    M -->|Pub/Sub| Jobs[Achtergrondtaken]
    C --> T[Template / Serializer]
    T -->|HTTP-antwoord| Browser

    classDef magnet fill:#f96,stroke:#c33,color:#fff,font-weight:bold
    classDef io fill:#fdd,stroke:#c66,color:#000
    class M magnet
    class DB,Email,Stripe,Jobs io
....

Het model werkt als een magneet. Alles belandt er omdat er geen andere plek is met een duidelijk mandaat.

[.notes]
--
* "MVC geeft je drie bakken: Model, View, Controller. Bedrijfslogica hoort in het Model. Externe services... ook in het Model, denk ik? Caching? Model. Achtergrondtaken? Model."
* "Het framework heeft het niet fout. Het tekent alleen geen lijnen *binnen* het Model."
* "En als een codebase groot genoeg wordt, is de afwezigheid van interne lijnen het probleem."
--

// ─────────────────────────────────────────────────────────────────────────────
// SECTIE 2 — HET INZICHT
// ─────────────────────────────────────────────────────────────────────────────
== Deel 2: Het Ene Inzicht

// ─────────────────────────────────────────────────────────────────────────────
// SLIDE — WETEN VS. DOEN
// ─────────────────────────────────────────────────────────────────────────────
=== Weten vs. Doen

Kijk nog eens naar die `register()`-methode. Ze doet twee volledig verschillende *soorten* dingen:

[cols="1a,1a", options="header"]
|===
| 🧠 *Weten* (pure logica) | 🔌 *Doen* (neveneffecten)

| Is dit e-maildomein toegestaan?
| Zoek bestaande gebruikers in de database

| Is het wachtwoord sterk genoeg?
| Hash het wachtwoord (bcrypt — langzaam by design)

| Moet dit account worden vergrendeld?
| Schrijf de vergrendeling naar de database

| Hoe lang moet deze sessie duren?
| Genereer een cryptografisch token

| Is deze inlogpoging verdacht?
| Stuur een beveiligingswaarschuwing per e-mail
|===

[.callout]
****
Elk item in de linkerkolom kan worden beantwoord met *alleen data*. +
Geen database. Geen netwerk. Geen tijd. Geen willekeur. +
Gewoon functies.
****

[.notes]
--
* "Dit is het aha-moment dat ik je wil geven: de linkerkolom is *wiskunde*. Het is gewoon logica."
* "Je kunt elk item in de linkerkolom testen door wat data-maps mee te geven en het resultaat te controleren."
* "De rechterkolom is waar de wereld erbij komt. Daar worden dingen duur en traag."
* "FC/IS zegt: laten we deze twee kolommen niet door elkaar mengen in dezelfde functie."
--

// ─────────────────────────────────────────────────────────────────────────────
// SLIDE — DE MUUR
// ─────────────────────────────────────────────────────────────────────────────
=== De Muur

[mermaid,format=svg,width=100%]
....
flowchart LR
    SHELL["🐚 Imperative Shell
    ───────────────────
    HTTP handlers
    Database adapter
    E-mail & cache
    Klok & UUID-generator"]

    CORE["🧠 Functional Core
    ───────────────────
    Validatieregels
    Auth-beslissingen
    Sessiebeleid
    Risicoscoring"]

    SHELL -- "data in, beslissing uit ➜" --> CORE
    CORE -. "❌  roept nooit terug" .-> SHELL

    style SHELL fill:#fde8e8,stroke:#c66,stroke-width:2px,color:#000,font-size:16px
    style CORE  fill:#e8fde8,stroke:#6a6,stroke-width:2px,color:#000,font-size:16px
....

De shell *orkestreert*. De core *denkt*. Nooit andersom.

[.notes]
--
* "De shell is een script. Het haalt data op, geeft het aan de core, krijgt een beslissing terug en handelt ernaar."
* "De core is een bibliotheek van pure functies. Geef het data, het geeft je antwoorden. Geen neveneffecten."
* "Deze ene regel — core roept nooit shell aan — maakt al het andere mogelijk."
--

// ─────────────────────────────────────────────────────────────────────────────
// SLIDE — TWEE REGELS, DAT IS ALLES
// ─────────────────────────────────────────────────────────────────────────────
=== Twee Regels, Dat Is Alles

[cols="1a,1a", options="header"]
|===
| ✅ Functional Core | ✅ Imperative Shell

| Alleen pure functies +
Geen I/O, geen logging, geen uitzonderingen voor flow control +
Zelfde input → zelfde output, altijd, voor altijd +
Afhankelijk van alleen data en andere pure functies

| Alle neveneffecten leven hier — en *alleen* hier +
Leest DB, roept API's aan, logt, hasht, verstuurt e-mails +
Levert alle "buitenwereld"-inputs aan de core +
Componeert core-aanroepen tot workflows
|===

IMPORTANT: Shell → Core is toegestaan. Core → Shell is *nooit* toegestaan.

Dat is het hele patroon. +
Serieus. Dat is het. De rest is implementatie.

[.notes]
--
* "Ik weet dat het te simpel klinkt. Maar de implicaties zijn enorm."
* "In Wagoe wordt deze regel afgedwongen door de linter. Een compile-time check. Als een `core/`-namespace iets vereist uit `shell/`, mislukt de build."
* "In Python en Ruby dwing je het af met code reviews en discipline. Minder betrouwbaar, maar nog steeds effectief."
--

// ─────────────────────────────────────────────────────────────────────────────
// SECTIE 3 — HEXAGONAAL
// ─────────────────────────────────────────────────────────────────────────────
== Deel 3: Hexagonale Architectuur

_Ja, de naam is verschrikkelijk. Het idee is briljant._

// ─────────────────────────────────────────────────────────────────────────────
// SLIDE — WAT HEXAGONAAL TOEVOEGT
// ─────────────────────────────────────────────────────────────────────────────
=== Wat Hexagonaal Toevoegt

FC/IS vertelt je *waar* logica leeft. Hexagonale Architectuur (Alistair Cockburn, 2005) vertelt je *hoe de shell verbinding maakt met de buitenwereld*.

Het kernidee: alle verbindingen met externe systemen verlopen via **ports**.

* Een **port** is een *interface* (een contract, een protocol, een ABC — hoe je taal het ook noemt)
* Een **adapter** is een *concrete implementatie* van die port
* Het domein kent alleen de port. Het importeert nooit een adapter.

[mermaid,format=svg,width=100%]
....
flowchart LR
    HTTP["🌐 HTTP"]
    CLI["💻 CLI"]
    TESTS["🧪 Tests"]

    SVC(["IUserService\n— input port —"])

    CORE["🧠 Bedrijfslogica\npure functies"]

    REPO(["IUserRepository\n— output port —"])
    MAIL(["EmailSenderProtocol\n— output port —"])

    PG[("PostgreSQL")]
    H2[("H2")]
    SMTP["📧 SMTP"]
    SES["☁️ AWS SES"]

    HTTP & CLI & TESTS --> SVC --> CORE
    CORE --> REPO & MAIL
    REPO --> PG & H2
    MAIL --> SMTP & SES

    classDef port fill:#fffde0,stroke:#bb8,color:#000,font-style:italic
    classDef core fill:#e8fde8,stroke:#6a6,color:#000,font-weight:bold
    classDef adapter fill:#f0f0f0,stroke:#999,color:#000
    class SVC,REPO,MAIL port
    class CORE core
    class HTTP,CLI,TESTS,PG,H2,SMTP,SES adapter
....

[.notes]
--
* "De testsuite is een driving adapter. Hij stuurt het domein aan via dezelfde interface als HTTP."
* "Dat is het kernidee: tests en productiecode gebruiken *dezelfde deur*."
* "En omdat de output ports gewoon interfaces zijn, kun je H2 inpluggen voor tests en PostgreSQL in productie. Geen codewijzigingen."
--

// ─────────────────────────────────────────────────────────────────────────────
// SLIDE — EEN PORT IN WAGOE
// ─────────────────────────────────────────────────────────────────────────────
=== Een Port in Wagoe

[source,clojure]
----
;; libs/user/src/wagoe/user/ports.clj

(defprotocol IUserRepository
  "Output port — de kijk van het domein op 'iets dat gebruikers opslaat'.
   Het weet niet of dat PostgreSQL, H2 of een map in geheugen is."

  (find-user-by-id    [this user-id])
  (find-user-by-email [this email])
  (create-user        [this user-data])
  (update-user        [this user-data])
  (soft-delete-user   [this user-id])
  (find-users         [this options]))
----

Vergelijk: in Django *is* je model de ORM. In Rails *erf* je van `ActiveRecord::Base`. +
Beide koppelen je bedrijfslogica aan een specifieke database-library op klasseniveau.

Een port zegt: "Ik heb *iets* nodig dat gebruikers kan vinden en opslaan. Ik geef niet om wat het is."

[.notes]
--
* "In Python gebruik je `typing.Protocol` of `abc.ABC`. In Ruby, duck typing + documentatie — minder expliciet maar werkbaar."
* "Het cruciale verschil: met een port kun je de implementatie vervangen zonder bedrijfslogica aan te raken."
* "Met Django ORM kun je het mocken. Dat is anders dan het vervangen."
--

// ─────────────────────────────────────────────────────────────────────────────
// SECTIE 4 — WAGOE: MODULARITEIT
// ─────────────────────────────────────────────────────────────────────────────
== Deel 4: Wagoe — Gebouwd op Modulariteit

_13 libraries. Één patroon. Elk onafhankelijk inzetbaar._

// ─────────────────────────────────────────────────────────────────────────────
// SLIDE — DE 13 LIBRARIES
// ─────────────────────────────────────────────────────────────────────────────
=== De 13 Libraries

Wagoe is geen monoliet. Het zijn 13 onafhankelijk publiceerbare libraries, elk gericht op *één ding*:

[cols="1,1,3", options="header"]
|===
| Categorie | Library | Wat het doet

.3+| *Fundament*
| `core`
| Validatie, hoofdletterconversie, interceptor-pipeline, feature flags

| `platform`
| HTTP-routing (Reitit), DB-adapters (PostgreSQL / MySQL / SQLite / H2), migraties, multi-DB-ondersteuning

| `observability`
| Request-logging, gestructureerde metrics, distributed tracing-interceptors

.3+| *Domein*
| `user`
| Authenticatie, sessies, MFA, wachtwoordbeleid, audittrail

| `admin`
| HTMX-aangedreven back-office UI, entiteitsconfiguratie, tabel/formulier-generatie

| `tenant`
| Multi-tenancy (schema-per-tenant), tenant-resolutie-middleware

.5+| *Infrastructuur*
| `cache`
| Redis + in-memory adapters, TTL-beheer, atomaire operaties

| `jobs`
| Achtergrondtaakverwerking, retry-logica, dead-letter queues

| `email`
| SMTP-verzending, async-wachtrijmodus, in-memory adapter voor tests

| `storage`
| Lokaal bestandssysteem + AWS S3-adapters, afbeeldingsverkleining, presigned URLs

| `realtime`
| WebSocket-verbindingen, pub/sub-messaging, SSE-ondersteuning

.1+| *Tooling*
| `scaffolder`
| Genereert een nieuw FC/IS-module met de juiste directorystructuur in één commando

.1+| *Externe services*
| `external`
| Adapters voor SMTP, IMAP, Stripe-betalingen & webhooks, en Twilio SMS/WhatsApp — elk achter zijn eigen port
|===

[.notes]
--
* "Elke library heeft zijn eigen `build.clj`, zijn eigen tests, zijn eigen versie. Je kunt alleen `wagoe/user` naar Clojars publiceren."
* "Je hoeft niet alle 13 te gebruiken. Pak wat je nodig hebt. Het patroon houdt ze consistent."
* "De scaffolder genereert een nieuwe library met de juiste FC/IS-structuur in één commando."
--

// ─────────────────────────────────────────────────────────────────────────────
// SLIDE — ELKE LIBRARY ZIET ER HETZELFDE UIT VAN BINNEN
// ─────────────────────────────────────────────────────────────────────────────
=== Elke Library Ziet Er Van Binnen Hetzelfde Uit

[source]
----
libs/{elke-library}/src/wagoe/{library}/
├── core/           ← pure functies. punt. einde verhaal.
│   ├── domain.clj
│   └── validation.clj
├── shell/          ← neveneffecten, I/O, orkestratie
│   ├── service.clj
│   ├── persistence.clj
│   └── web_handlers.clj
├── ports.clj       ← interface-definities
└── schema.clj      ← datavormen (Malli)
----

Consistentie is een feature. +
Open welke library dan ook. Je weet al waar je de bedrijfsregels moet zoeken. +
Je weet al waar de database-code leeft. +
Geen schatzoekerij.

[.notes]
--
* "Dit is wat ik bedoel met 'afgedwongen door structuur.' Het patroon ís de directoryindeling."
* "Nieuwe developer komt het team binnen. Ze leren het patroon één keer. Ze begrijpen elke module."
* "Vergelijk dit met een Django-project waar elke app zijn eigen conventies heeft over waar logica leeft."
--

// ─────────────────────────────────────────────────────────────────────────────
// SLIDE — MODULES COMPONEREN CLEAN
// ─────────────────────────────────────────────────────────────────────────────
=== Modules Componeren clean

Omdat elke module een *port* (interface) blootstelt, kunnen ze worden gekoppeld zonder strakke binding.
De `user`-service kent alleen interfaces — het heeft geen idee wat erachter zit:

[mermaid,format=svg,width=100%]
....
flowchart LR
    SVC["user/service"]

    subgraph ports["Ports (interfaces waarvan de service afhankelijk is)"]
        REPO(["IUserRepository"])
        MAIL(["EmailSenderProtocol"])
        CACHE(["ICache"])
        JOBS(["IJobQueue"])
    end

    subgraph prod["Productie-adapters"]
        PG[("PostgreSQL")]
        SMTP["SMTP"]
        REDIS["Redis"]
        WORKER["Taakverwerker"]
    end

    subgraph test["Test-adapters"]
        H2[("H2")]
        FAKE_MAIL["In-memory"]
        FAKE_CACHE["In-memory"]
        INLINE["Inline (sync)"]
    end

    SVC --> REPO & MAIL & CACHE & JOBS

    REPO -.->|prod| PG
    MAIL -.->|prod| SMTP
    CACHE -.->|prod| REDIS
    JOBS -.->|prod| WORKER

    REPO -.->|test| H2
    MAIL -.->|test| FAKE_MAIL
    CACHE -.->|test| FAKE_CACHE
    JOBS -.->|test| INLINE

    classDef port fill:#fffde0,stroke:#bb8,color:#000,font-style:italic
    classDef svc fill:#e8fde8,stroke:#6a6,color:#000,font-weight:bold
    classDef prod_a fill:#e8e8fd,stroke:#66a,color:#000
    classDef test_a fill:#f0f0f0,stroke:#999,color:#555
    class REPO,MAIL,CACHE,JOBS port
    class SVC svc
    class PG,SMTP,REDIS,WORKER prod_a
    class H2,FAKE_MAIL,FAKE_CACHE,INLINE test_a
....

De applicatie verstuurt e-mail via `EmailSenderProtocol` (in `libs/email`). De aanroeper heeft *geen idee* of dat SMTP of AWS SES is.

[.notes]
--
* "Dit is losse koppeling in de praktijk. Niet als principe. Als daadwerkelijk structureel resultaat."
* "Wil je overstappen van SMTP naar AWS SES? Verander één regel in de config. Nul wijzigingen in applicatiecode."
* "Dit is niet dependency injection als een testtruc. Het is dependency injection als een echt architectuurgereedschap."
--

// ─────────────────────────────────────────────────────────────────────────────
// SECTIE 5 — UITWISSELBAARHEID
// ─────────────────────────────────────────────────────────────────────────────
== Deel 5: Uitwisselbaarheid — Verwissel Alles

_Als het de port implementeert, werkt het._

// ─────────────────────────────────────────────────────────────────────────────
// SLIDE — DE DATABASE IS GEWOON EEN ADAPTER
// ─────────────────────────────────────────────────────────────────────────────
=== De Database Is Gewoon een Adapter

Wagoe wordt geleverd met adapters voor vier databases — die allemaal dezelfde port implementeren:

[source,clojure]
----
;; Vier adapters. Één port. Nul wijzigingen in bedrijfslogica als je wisselt.

;; PostgreSQL / H2 / MySQL / SQLite adapter — zelfde code, andere driver
(defrecord SQLUserRepository [ctx]           ; ctx bevat db-verbinding + dialect
  IUserRepository
  (find-user-by-email [_ email]
    (db/execute-one! ctx                     ; next.jdbc onder de motorkap
      {:select [:id :email :password-hash :active :role]
       :from   [:auth-users]
       :where  [:= :email email]})))         ; HoneySQL map — geparametriseerd, geen string-concat

;; In-memory adapter — ultrasnelle unit tests, nul I/O
(defrecord InMemoryUserRepository [store]
  IUserRepository
  (find-user-by-email [_ email]
    (first (filter #(= email (:email %)) (vals @store)))))
----

De HoneySQL-map compileert naar een geparametriseerde prepared statement — `[:= :email email]` doet nooit string-concatenatie.

[.notes]
--
* "In Django *is* je model de ORM. Als je zonder database wilt testen, mock je de ORM. Dat is shimmen."
* "In Wagoe is de database een stekker. Je trekt de ene stekker eruit en steekt een andere erin. Geen shimmen nodig."
* "De HoneySQL-map vermijdt raw SQL-strings volledig — geen SQL-injection-aanvalsoppervlak, en dezelfde map werkt voor alle vier databasedialecten."
* "De in-memory adapter maakt unit tests voor shell-services triviaal — geen Docker, geen migraties."
--

// ─────────────────────────────────────────────────────────────────────────────
// SLIDE — "WIRING" IS GEWOON CONFIGURATIE
// ─────────────────────────────────────────────────────────────────────────────
=== "Wiring" Is Gewoon Configuratie

[source,clojure]
----
;; resources/conf/prod/config.edn — productie
{:user/repository  {:type :postgresql  :db #ig/ref :db/connection}
 :email/service    {:type :smtp        :host "mail.example.com"}
 :cache/service    {:type :redis       :url "redis://cache:6379"}
 :user/service     {:repository  #ig/ref :user/repository
                    :email       #ig/ref :email/service
                    :cache       #ig/ref :cache/service}}

;; resources/conf/test/config.edn — tests
{:user/repository  {:type :h2          :db #ig/ref :db/test-connection}
 :email/service    {:type :in-memory}
 :cache/service    {:type :in-memory}
 :user/service     {:repository  #ig/ref :user/repository
                    :email       #ig/ref :email/service
                    :cache       #ig/ref :cache/service}}
----

De bedrijfslogica in `:user/service` is *identiek* in beide configs. +
Het enige dat verandert is wat er wordt ingeplugd.

[.notes]
--
* "Integrant bedraadt het systeem bij opstarten vanuit config. Het lijkt een beetje op Django's settings.py maar dan voor componentkoppeling."
* "In Django betekent overstappen van SMTP naar SES zoeken door `send_mail`-aanroepen. Hier is het één woord in een config-bestand."
* "De testconfig gebruikt overal in-memory. Tests draaien in milliseconden. Geen Docker-daemon vereist."
--

// ─────────────────────────────────────────────────────────────────────────────
// SLIDE — WAT JE KUNT VERWISSELEN
// ─────────────────────────────────────────────────────────────────────────────
=== Wat Je Kunt Verwisselen Zonder Bedrijfslogica Aan Te Raken

[cols="1,2,1", options="header"]
|===
| Capability | Beschikbare adapters | Wat je wijzigt

| **Database**
| PostgreSQL, MySQL, SQLite, H2
| Één regel in config

| **E-mail**
| SMTP, async-wachtrij, in-memory (tests)
| Één regel in config

| **Cache**
| Redis, in-memory (tests)
| Één regel in config

| **Opslag**
| Lokaal bestandssysteem, AWS S3
| Één regel in config

| **Achtergrondtaken**
| Taakverwerker, inline (tests)
| Één regel in config

| **Afleverkanaal**
| HTTP, CLI, testrunner
| Ander startpunt
|===

[.notes]
--
* "Dit is niet theoretisch. Ik gebruik H2 in alle tests. Overstappen van H2 naar PostgreSQL is één woord: `:type :postgresql`."
* "Elke adapter wordt gevalideerd tegen een contract-testsuite — dezelfde tests, andere stekker."
* "Dezelfde gebruikersregistratiecode draait vanuit HTTP, CLI en tests. Geen duplicatie."
--

// ─────────────────────────────────────────────────────────────────────────────
// SECTIE 6 — ECHTE CODE
// ─────────────────────────────────────────────────────────────────────────────
== Deel 6: Echte Code, Naast Elkaar

// ─────────────────────────────────────────────────────────────────────────────
// SLIDE — GEBRUIKERSREGISTRATIE: DJANGO
// ─────────────────────────────────────────────────────────────────────────────
=== Gebruikersregistratie: Django

[source,python]
----
# views.py — een volkomen normale Django-view die te veel doet
def register(request):
    form = RegisterForm(request.POST)
    if not form.is_valid():
        return render(request, "register.html", {"form": form})

    email    = form.cleaned_data["email"]
    password = form.cleaned_data["password"]

    # Bedrijfsregel... in de view... want waar anders?
    if User.objects.filter(email=email).exists():
        form.add_error("email", "Al in gebruik")
        return render(request, "register.html", {"form": form})

    user = User.objects.create_user(email=email, password=password)

    send_welcome_email.delay(user.pk)    # fire and hope
    stripe.Customer.create(email=email)  # ook fire and hope

    return redirect("dashboard")
----

Vragen die je niet kunt beantwoorden door dit te lezen:

* Als `send_welcome_email` faalt, wordt de gebruiker dan toch aangemaakt?
* Waar is de "domein niet toegestaan"-check? In het formulier? Het model? Allebei?
* Hoe test je dit zonder Stripe, Celery en een testdatabase?

[.notes]
--
* "Dit is geen slechte code. Dit is volkomen normale, pragmatische Django. Het probleem is structureel."
* "De 'e-mail al in gebruik'-check is een bedrijfsregel. Hij leeft in de view omdat dat de weg van de minste weerstand is."
* "Test dit: je hebt `@pytest.mark.django_db`, mock Stripe, mock Celery, mock de e-mailbackend nodig."
--

// ─────────────────────────────────────────────────────────────────────────────
// SLIDE — GEBRUIKERSREGISTRATIE: WAGOE CORE
// ─────────────────────────────────────────────────────────────────────────────
=== Gebruikersregistratie: Wagoe Core (Pure Logica)

[source,clojure]
----
;; libs/user/src/wagoe/user/core/user.clj
;; Dit bestand heeft nul imports van welke I/O-library dan ook. Nooit.

(defn check-duplicate-user-decision
  "Is registratie toegestaan voor dit e-mailadres?

   De shell doet de databasequery en geeft het resultaat door.
   Deze functie raakt nooit een database aan — ze neemt alleen een beslissing."
  [user-data existing-user]
  (if existing-user
    {:decision :reject
     :reason   :duplicate-email
     :email    (:email user-data)}
    {:decision :proceed}))

(defn prepare-user-for-creation
  "Bouw het volledige gebruikersrecord klaar voor opslaan.

   De shell levert current-time en user-id.
   Deze functie roept nooit de klok of een UUID-generator aan."
  [user-data current-time user-id]
  (merge user-data
         {:id                  user-id
          :created-at          current-time
          :active              true
          :login-count         0
          :failed-login-count  0}))
----

Test voor `check-duplicate-user-decision`: één regel. Geen mocks. Geen database. Draait in 0,1 ms.

[.notes]
--
* "Geen ORM-import. Geen database-client. Geen netwerk. Gewoon een functie die data neemt en data teruggeeft."
* "`current-time` en `user-id` komen van de shell. De core weet niet hoe laat het is — en dat is de bedoeling."
* "Deze functie is volledig eerlijk over wat ze nodig heeft. Geen verborgen dependencies."
--

// ─────────────────────────────────────────────────────────────────────────────
// SLIDE — GEBRUIKERSREGISTRATIE: WAGOE SHELL
// ─────────────────────────────────────────────────────────────────────────────
=== Gebruikersregistratie: Wagoe Shell (Orkestratie)

[source,clojure]
----
;; libs/user/src/wagoe/user/shell/service.clj — vereenvoudigd
;; De shell is een script: ophalen → beslissen → handelen → herhalen.

(defn register-user! [repositories user-data]
  (let [{:keys [user-repo audit-repo]} repositories]

    ;; Stap 1: CORE — pure validatie, geen I/O
    (let [result (user-core/validate-user-creation-request user-data config)]
      (when-not (:valid? result)
        (throw (ex-info "Ongeldige data" {:errors (:errors result)}))))

    ;; Stap 2: SHELL haalt op, CORE beslist — logica blijft puur
    (let [existing (user-repo/find-user-by-email user-repo (:email user-data))
          decision (user-core/check-duplicate-user-decision user-data existing)]
      (when (= :reject (:decision decision))
        (throw (ex-info "Gebruiker bestaat al" {}))))

    ;; Stap 3: SHELL doet de dure I/O, geeft resultaten door aan CORE
    (let [hash     (auth-shell/hash-password (:password user-data))  ;; bcrypt hier
          now      (current-timestamp)    ;; shell bezit de klok
          id       (generate-user-id)     ;; shell bezit willekeurigheid
          prepared (user-core/prepare-user-for-creation
                     (assoc user-data :password-hash hash) now id)

          ;; Stap 4: SHELL slaat op
          created  (user-repo/create-user user-repo prepared)]

      (dissoc created :password-hash))))  ;; lek de hash nooit
----

[.notes]
--
* "De shell is bijna saai. Het is een recept: haal ingrediënten op, roep een core-functie aan, sla het resultaat op."
* "Alle interessante logica zit in de core. De shell orkestreert alleen."
* "Let op: `current-timestamp` en `generate-user-id` zijn gedefinieerd in de shell. De core krijgt het *resultaat*, niet de functie."
--

// ─────────────────────────────────────────────────────────────────────────────
// SECTIE 7 — AHA-MOMENTEN
// ─────────────────────────────────────────────────────────────────────────────
== Deel 7: Vijf Aha-momenten

_De momenten die al die late nachten de moeite waard maakten._

// ─────────────────────────────────────────────────────────────────────────────
// SLIDE — AHA #1: TESTEN ZONDER MOCKS
// ─────────────────────────────────────────────────────────────────────────────
=== Aha #1 — Je Tests Stoppen Met Liegen

.Wagoe — hoe een test er werkelijk uitziet
[source,clojure]
----
;; libs/user/test/wagoe/user/core/authentication_test.clj

(def active-user {:id "uuid-1"  :email "alice@example.com"
                  :active true  :role :user
                  :deleted-at nil  :failed-login-count 0})

(deftest login-attempt-policy
  (testing "blokkeert verwijderde accounts"
    (let [result (auth/should-allow-login-attempt?
                   (assoc active-user :deleted-at some-past-time)
                   config now)]
      (is (false? (:allowed? result)))))

  (testing "blokkeert vergrendelde accounts"
    (let [result (auth/should-allow-login-attempt?
                   (assoc active-user :lockout-until some-future-time)
                   config now)]
      (is (false? (:allowed? result)))))

  (testing "staat normale actieve accounts toe"
    (is (true? (:allowed? (auth/should-allow-login-attempt? active-user config now))))))
----

Geen `@pytest.mark.django_db`. Geen `@patch`. Geen factory. Geen fixtures. Geen container. +
Gewoon data in, data uit. Draait in **minder dan 5 milliseconden**.

[.notes]
--
* "De hele `core/authentication`-testsuite draait in ongeveer 10 ms. Allemaal."
* "Als tests zo snel en simpel zijn, draaien developers ze echt. Constant. Bij elke opslag."
* "En ze testen de *echte* bedrijfsregels, niet de mock van de mock van de database-client."
--

// ─────────────────────────────────────────────────────────────────────────────
// SLIDE — AHA #2: TIJD IS GEWOON DATA
// ─────────────────────────────────────────────────────────────────────────────
=== Aha #2 — Tijd Is Gewoon een Parameter

.Het verborgen dependency-probleem
[source,python]
----
# Django — tijd is een verborgen globale
def is_session_valid(session):
    return session.expires_at > datetime.now()  # ← waar komt "nu" vandaan?
----

.Wagoe — tijd is expliciet
[source,clojure]
----
;; libs/user/src/wagoe/user/core/session.clj

(defn is-session-valid?
  "current-time wordt geïnjecteerd door de shell. De core leest nooit de klok."
  [session current-time]
  (cond
    (nil? session)                                  {:valid? false :reason "Niet gevonden"}
    (.isAfter current-time (:expires-at session))   {:valid? false :reason "Verlopen"}
    :else                                           {:valid? true}))
----

[source,clojure]
----
;; Test — jij beheert de tijd. Geen freezegun. Geen Timecop. Geen monkey-patching.
(deftest session-verloop
  (let [verlopen {:expires-at (Instant/parse "2020-01-01T00:00:00Z")}
        nu       (Instant/parse "2026-01-01T00:00:00Z")]
    (is (false? (:valid? (session/is-session-valid? verlopen nu))))))
----

Dezelfde truc werkt voor UUID's, willekeurige tokens, externe API-antwoorden — alles wat de "echte wereld" levert.

[.notes]
--
* "Geen `freezegun`. Geen `Timecop`. Geen `mock.patch('datetime.datetime.now')`."
* "Tijd expliciet maken klinkt lastig. In de praktijk kost het 10 seconden en bespaart het uren bij het debuggen van flaky tests."
* "Als je dit eenmaal doet, merk je hoeveel andere verborgen globale dependencies je code heeft."
--

// ─────────────────────────────────────────────────────────────────────────────
// SLIDE — AHA #3: COMPLEXE LOGICA WORDT LEESBAAR
// ─────────────────────────────────────────────────────────────────────────────
=== Aha #3 — Complexe Domeinlogica Wordt Leesbaar

Hier is Wagoe's inlog-risicoscorer. Lees het als een specificatie.

[source,clojure]
----
(defn analyze-login-risk
  "Score the risk of a login attempt. No database, no HTTP, no Redis — just data."
  [user login-context recent-sessions now]
  (let [risk-factors
        (cond-> []
          ;; Inloggen vanaf een nieuw IP-adres? Verdacht.
          (not-any? #(= (:ip-address login-context) (:ip-address %)) recent-sessions)
          (conj {:factor :new-ip-address :weight 30})

          ;; Admin-account? Hogere drempel.
          (= :admin (:role user))
          (conj {:factor :admin-user :weight 25})

          ;; Account al 30 dagen niet gebruikt? Slaap-verdacht.
          (and (:last-login user)
               (> (days-since (:last-login user) now) 30))
          (conj {:factor :dormant-account :weight 40}))

        score (reduce + (map :weight risk-factors))]

    {:risk-score    score
     :risk-factors  risk-factors
     :requires-mfa? (> score 50)}))
----

Test elk scenario met een map en een tijdstempel. Geen Redis-mock. Geen HTTP-context. Geen sessie-opslag.

[.notes]
--
* "Dit is beveiligingskritieke logica. Je wilt het kunnen lezen, beredeneren en verifiëren."
* "Als het een pure functie is, kan een product manager naar de code wijzen en zeggen 'die drempel moet 40 zijn, niet 50.' En ze kunnen gelijk hebben."
* "Probeer dit in een Django-view zonder het verstrengeld te raken met het request-object."
--

// ─────────────────────────────────────────────────────────────────────────────
// SLIDE — AHA #4: ÉÉN CODEBASE, DRIE AFLEVERKANALEN
// ─────────────────────────────────────────────────────────────────────────────
=== Aha #4 — Één Bedrijfslogica, Drie Startpunten

Dezelfde `user-core`-functies sturen drie volledig verschillende afleverkanalen aan:

[mermaid,format=svg,width=100%]
....
flowchart LR
    subgraph drivers["Startpunten"]
        WEB["🌐 HTTP handler"]
        CLI["💻 CLI commando"]
        TEST["🧪 Testsuite"]
    end

    subgraph core["🧠 Functional Core — gedeeld door alle drie"]
        AUTH["core/authentication"]
        USER["core/user"]
        SESSION["core/session"]
    end

    WEB --> AUTH & USER & SESSION
    CLI --> AUTH & USER
    TEST --> AUTH & USER & SESSION

    classDef core fill:#e8fde8,stroke:#6a6,color:#000,font-weight:bold
    class AUTH,USER,SESSION core
....

Geen copy-paste. Geen "de CLI-versie doet het net iets anders." +
Één set bedrijfsregels, geleverd zoals je wilt.

[.notes]
--
* "Dit is enorm in de praktijk. Hoeveel Django-projecten hebben een management command dat de logica uit de view subtiel opnieuw implementeert?"
* "In Wagoe roepen zowel het CLI-startpunt als de HTTP-handler dezelfde shell-service aan, die dezelfde core aanroept."
* "Voeg een nieuw afleverkanaal toe — een GraphQL API, een gRPC-endpoint — en je krijgt de bedrijfslogica gratis."
--

// ─────────────────────────────────────────────────────────────────────────────
// SLIDE — AHA #5: DE CORE IS JE SPECIFICATIE
// ─────────────────────────────────────────────────────────────────────────────
=== Aha #5 — De Core Is Je Levende Specificatie

[source,clojure]
----
;; Lees dit. Je kent nu het volledige wachtwoordbeleid.
;; Geen Confluence-pagina. Geen Jira-ticket. Geen "vraag Sarah, zij heeft het geschreven."

(defn meets-password-policy? [password policy user-context]
  (let [violations
        (cond-> []
          (< (count password) (:min-length policy 8))
          (conj {:code :too-short  :message "Minimaal 8 tekens"})

          (and (:require-uppercase policy)
               (not (re-find #"[A-Z]" password)))
          (conj {:code :missing-uppercase  :message "Een hoofdletter vereist"})

          (and (:require-numbers policy)
               (not (re-find #"\d" password)))
          (conj {:code :missing-number  :message "Een cijfer vereist"})

          ;; Persoonlijk tintje: je eigen e-mailnaam mag niet je wachtwoord zijn
          (and user-context
               (str/includes? (str/lower-case password)
                              (str/lower-case (email-username (:email user-context)))))
          (conj {:code :contains-email  :message "Mag je e-mailadres niet bevatten"}))]

    {:valid? (empty? violations)  :violations violations}))
----

Een product manager kan dit lezen. Een beveiligingsauditor kan het verifiëren. Een nieuwe teamlid kan het begrijpen — zonder dat de database draait.

[.notes]
--
* "Geen ORM-mixin. Geen Rails concern. Geen callback-keten. Geen 'het valideert ergens, ik denk in het formulier?'"
* "De regels staan op één plek, uitgedrukt als code, verifieerbaar als tests."
* "Dit is wat 'de code is de documentatie' er werkelijk uitziet als het werkt."
--

// ─────────────────────────────────────────────────────────────────────────────
// SECTIE 8 — EERLIJKE AFWEGINGEN
// ─────────────────────────────────────────────────────────────────────────────
== Deel 8: Is Het De Moeite Waard?

_Het eerlijke antwoord._

// ─────────────────────────────────────────────────────────────────────────────
// SLIDE — WAAR HET UITBLINKT
// ─────────────────────────────────────────────────────────────────────────────
=== Waar FC/IS + Hexagonaal Echt Loont

[cols="1,2", options="header"]
|===
| Context | Waarom het helpt

| **Rijke domeinlogica**
| Regels zijn geïsoleerd, leesbaar, testbaar. Geen infrastructuur nodig om ze te begrijpen.

| **Meerdere afleverkanalen**
| HTTP, CLI, taken — dezelfde core, geen duplicatie.

| **Infrastructuuronzekerheid**
| Van database wisselen? Cloud-providers? De adapters absorberen de wijziging.

| **Beveiligingskritieke code**
| Vergrendeling, rate limiting, risicoscoring: pure functies zijn eenvoudig te auditen en verifiëren.

| **Langlevende teams**
| Nieuwe developers leren het patroon één keer. Elke module is navigeerbaar.

| **Library/framework-ontwikkeling**
| Het patroon is hoe Wagoe zelf is gestructureerd. Werkt prachtig voor herbruikbare code.
|===

[.notes]
--
* "Het rendement op investering neemt toe naarmate de tijd verstrijkt. Jaar 1 van een project wint Django op ergonomie. Jaar 4 begint de architectuurschuld zichtbaar te worden."
* "Als je iets bouwt dat 3+ jaar door verschillende teams wordt onderhouden, is deze investering de moeite waard."
--

// ─────────────────────────────────────────────────────────────────────────────
// SLIDE — WAAR HET JE GELD KOST
// ─────────────────────────────────────────────────────────────────────────────
=== Waar Het Je Geld Kost

[cols="1,2", options="header"]
|===
| Kosten | Eerlijk beeld

| **Meer bestanden, meer lagen**
| Ja. Elke feature raakt minimaal `core/`, `shell/` en `ports.clj`. Dat zijn drie bestanden in plaats van één.

| **Geen ORM-shortcuts**
| `User.where(active: true).count` wordt een port-methode + SQL. Meer typen vooraf.

| **Django Admin verdwijnt**
| Je kunt Django Admin niet op een port richten. Je verliest dat gratis.

| **Spring devs: DI is niet genoeg**
| Spring's `@Autowired` helpt met adapters maar verhindert niet dat je logica en I/O mixt in dezelfde `@Service`. FC/IS voegt de regel toe die DI werkelijk nuttig maakt.

| **Het patroon vereist discipline**
| In Python/Ruby/Java houdt niets een junior developer tegen om de ORM in `core/` te importeren. Je hebt code reviews of linting nodig om het af te dwingen.

| **Simpele CRUD heeft dit niet nodig**
| Een app met 10 modellen en geen echte bedrijfslogica? Django admin + DRF, of Spring Boot + JPA. Overdrijf het niet.
|===

[.notes]
--
* "Wees eerlijk tegen je team. Dit is een ruil. Meer structuur nu, minder pijn later."
* "Als je deadline volgende week is en je bouwt een blog, gebruik Django en ga op tijd naar huis."
* "Als je de vierde iteratie van een betalingssysteem bouwt dat in 2030 nog moet bestaan, denk hier dan over na."
--

// ─────────────────────────────────────────────────────────────────────────────
// SLIDE — JE KUNT DIT VANDAAG DOEN IN PYTHON
// ─────────────────────────────────────────────────────────────────────────────
=== Je Kunt Dit Vandaag Doen in Python

Geen Clojure vereist. Alleen een conventie.

[source,python]
----
# core/authentication.py — GEEN Django-imports. Nul.
from dataclasses import dataclass
from datetime import datetime
from typing import Optional

@dataclass(frozen=True)
class LoginBeslissing:
    toegestaan: bool
    reden:      Optional[str] = None

def mag_inloggen(gebruiker_data: dict,
                 config: dict,
                 nu: datetime) -> LoginBeslissing:
    if gebruiker_data is None:
        return LoginBeslissing(toegestaan=False, reden="Niet gevonden")
    if not gebruiker_data["active"]:
        return LoginBeslissing(toegestaan=False, reden="Gedeactiveerd")
    if gebruiker_data.get("lockout_until") and nu < gebruiker_data["lockout_until"]:
        return LoginBeslissing(toegestaan=False, reden="Vergrendeld")
    return LoginBeslissing(toegestaan=True)
----

[source,python]
----
# shell/user_service.py — Django-bewust. Roept core aan. Doet I/O.
from core.authentication import mag_inloggen
from myapp.models import User
from datetime import datetime

def authenticeer(email: str, password: str, nu=None) -> dict:
    nu       = nu or datetime.now()
    gebruiker = User.objects.filter(email=email).first()
    return mag_inloggen(gebruiker.__dict__ if gebruiker else None, config, nu)
----

[.notes]
--
* "De regel: `core/` importeert nooit iets van Django of Rails. Code reviews dwingen het af."
* "Python's `typing.Protocol` laat je ports netjes definiëren. Ruby doet het met duck typing."
* "Je krijgt niet de compile-time afdwinging die Clojure biedt, maar je haalt het meeste voordeel eruit."
--

// ─────────────────────────────────────────────────────────────────────────────
// SLIDE — JE KUNT DIT VANDAAG DOEN IN JAVA/SPRING
// ─────────────────────────────────────────────────────────────────────────────
=== Je Kunt Dit Vandaag Doen in Java/Spring

.Core — gewoon Java, nul Spring-imports
[source,java]
----
// core/UserRegistrationCore.java — geen @Service, geen @Autowired, niets
public final class UserRegistrationCore {

    // Onveranderlijk resultaattype — records zijn compact in Java 16+
    public record RegistratieBeslissing(boolean toegestaan, String reden) {
        public static RegistratieBeslissing doorgaan() {
            return new RegistratieBeslissing(true, null);
        }
        public static RegistratieBeslissing weigeren(String reden) {
            return new RegistratieBeslissing(false, reden);
        }
    }

    // Pure functie: heeft de shell een bestaande gebruiker gevonden? Wat moet er gebeuren?
    public static RegistratieBeslissing checkDuplicaat(String email, boolean bestaatAl) {
        if (bestaatAl) return RegistratieBeslissing.weigeren("E-mail al in gebruik");
        return RegistratieBeslissing.doorgaan();
    }

    // Pure functie: bouw het volledige gebruikersrecord — shell levert tijd en ID
    public static GebruikersRecord voorbereidAanmaken(String email, String passwordHash,
                                                      UUID id, Instant aangemaaktOp) {
        return new GebruikersRecord(id, email, passwordHash, aangemaaktOp, true, 0);
    }
}
----

.Shell — Spring-service, orkestreert I/O, roept core aan
[source,java]
----
@Service
public class UserShellService {

    private final UserRepository  userRepository;
    private final PasswordEncoder passwordEncoder;

    public UserShellService(UserRepository repo, PasswordEncoder encoder) {
        this.userRepository  = repo;
        this.passwordEncoder = encoder;
    }

    public User registreer(RegisterRequest req) {
        // Stap 1: shell doet DB-query, core neemt de beslissing
        boolean bestaatAl = userRepository.existsByEmail(req.email());
        var beslissing    = UserRegistrationCore.checkDuplicaat(req.email(), bestaatAl);
        if (!beslissing.toegestaan()) throw new EmailAlreadyExistsException(beslissing.reden());

        // Stap 2: shell doet I/O, core bereidt het record voor
        String hash    = passwordEncoder.encode(req.password());
        var prepared   = UserRegistrationCore.voorbereidAanmaken(
                             req.email(), hash, UUID.randomUUID(), Instant.now());

        // Stap 3: shell slaat op
        return userRepository.save(prepared.toEntity());
    }
}
----

.Test van core — geen Spring, geen Mockito, helemaal niets
[source,java]
----
class UserRegistrationCoreTest {     // gewoon JUnit 5, geen annotaties

    @Test void weigertDuplicaatEmail() {
        var b = UserRegistrationCore.checkDuplicaat("alice@example.com", true);
        assertFalse(b.toegestaan());
        assertEquals("E-mail al in gebruik", b.reden());
    }

    @Test void staatNieuwEmailToe() {
        var b = UserRegistrationCore.checkDuplicaat("alice@example.com", false);
        assertTrue(b.toegestaan());
    }
}
// Draait in ~5 ms. Geen Spring-context. Geen Mockito. Geen database. Geen Stripe.
----

[.notes]
--
* "Java-devs: je kent dit patroon al. Het is eigenlijk een statische utility-klasse met goede returntypes."
* "De `UserRepository`-interface is de port. Spring Data JPA is de adapter. Je schrijft het al zo — de stap is alleen zorgen dat je *bedrijfslogica* niet in de service lekt."
* "Records (Java 16+) maken onveranderlijke resultaattypes bijna zo compact als Clojure-maps."
* "`Instant.now()` verhuist naar de shell. Nu kun je elk tijdstempel meegeven aan `voorbereidAanmaken` in tests — geen `Mockito.mockStatic`."
--

// ─────────────────────────────────────────────────────────────────────────────
// SLIDE — PROBEER HET IN ÉÉN MODULE
// ─────────────────────────────────────────────────────────────────────────────
== Probeer Het in Één Module

Kies *één ding* in je huidige codebase. Een model met echte bedrijfslogica. Een service die te veel doet.

. Maak `core/dat_ding.py` / `core/DatDingCore.java` / `core/dat_ding.rb`. **Geen framework-imports toegestaan.** Geen.
. Verplaats elke bedrijfsregel daarin als een gewone functie of statische methode.
. Laat de functies hun dependencies accepteren als argumenten — tijd, bestaande data, config.
. Schrijf tests voor die functies met *geen* mocks.
. Bouw een dunne service/shell bovenaan die de I/O doet en de pure functies aanroept.

Dat is het. Één module. Kijk hoe het voelt.

[quote]
____
Je hebt geen Clojure nodig. Je hebt Wagoe niet nodig. +
Je hebt alleen een `core/`-map nodig en de discipline om hem schoon te houden.
____

[.notes]
--
* "Ik probeer je niet naar Clojure te bekeren. Ik probeer je één extra architectuurgereedschap te geven."
* "Het patroon werkt in elke taal. De afdwinging varieert. De helderheid is er altijd."
* "Bereid om samen te werken met iedereen die het op hun codebase wil proberen."
--

// ─────────────────────────────────────────────────────────────────────────────
// SLIDE — BRONNEN
// ─────────────────────────────────────────────────────────────────────────────
== Bronnen

* **Gary Bernhardt** — _"Boundaries"_ (destroyallsoftware.com, 2012) +
  De lezing die dit alles in gang heeft gezet. 35 minuten. Verplichte kost.

* **Alistair Cockburn** — _"Hexagonal Architecture"_ (alistair.cockburn.us, 2005) +
  Waar ports en adapters hun naam vandaan krijgen.

* **Mark Seemann** — _"Functional architecture: the pits of success"_ (NDC 2016) +
  Een goede brug tussen FP-ideeën en praktische architectuur.

* **Wagoe framework** — deze repository +
  Elke `libs/*/src/*/core/`-map is een levend voorbeeld.

* **Beste startpunt** — https://github.com/wagoebv/wagoe-examples/tree/main/ecommerce-api[wagoe-examples/ecommerce-api] +
  De meest complete referentie-implementatie.

[.notes]
--
* "Gary Bernhardt's lezing is waar ik iedereen naartoe zou sturen die dieper wil gaan. Kijk ernaar voordat je het patroon probeert toe te passen."
* "Alistair Cockburn bedacht 'Hexagonaal' maar de post is dicht. Mark Seemann's lezing is toegankelijker."
--

// ─────────────────────────────────────────────────────────────────────────────
// SLIDE — VRAGEN?
// ─────────────────────────────────────────────────────────────────────────────
== Vragen?

[cols="3*", frame=none, grid=none]
|===
| 🧠 Core = *weten* | 🐚 Shell = *doen* | 🔌 Port = *contract*
|===

[quote]
____
Het meest radicale wat je kunt doen is scheiden wat je code *weet* van wat het *doet*.
____

— En er dan een muur tussen zetten.

[.notes]
--
* "Ik verwacht weerstand. 'Onze modellen zijn niet zo erg.' Eerlijk — totdat ze dat wel zijn."
* "Goede uitdagingsvraag: 'Hoe ga je om met transacties die meerdere operaties omvatten?' Antwoord: de shell bezit transactieboundaries, de core ziet ze nooit."
* "Nog een: 'Wat doen we met Django signals?' Signals zijn callbacks onder een andere naam — ze doorbreken de muur. In FC/IS maak je die effecten expliciet in de shell."
* "Laat 15+ minuten voor Q&A. Dit publiek heeft sterke meningen. Dat is goed."
--
