#
# Boundary Framework - Environment Configuration Template
# Copy this file to .env and customize for your environment
#

# =============================================================================
# Database Configuration
# =============================================================================
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=wagoe_dev
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# =============================================================================
# Application Configuration
# =============================================================================
HTTP_PORT=3000
HTTP_HOST=0.0.0.0
PROFILE=dev

# Default tenant ID for development
# Production systems should NOT rely on defaults
DEFAULT_TENANT_ID=00000000-0000-0000-0000-000000000001

# =============================================================================
# Port Management Configuration
# =============================================================================

# Main application port range for conflict resolution
HTTP_PORT_RANGE_START=3001
HTTP_PORT_RANGE_END=3010

# Documentation server port  
DOCS_PORT=8080

# =============================================================================
# Development Configuration
# =============================================================================

# Docker build configuration
BUILD_TARGET=runtime
VERSION=dev

# JVM Configuration for Docker
JAVA_OPTS=-Xmx512m -Xms128m -XX:+UseG1GC -XX:+UseContainerSupport

# =============================================================================
# Observability Configuration (Optional)
# =============================================================================

# Logging
LOG_LEVEL=info
LOG_FORMAT=json

# Metrics (if using Prometheus)
METRICS_PORT=9090
METRICS_PATH=/metrics

# Error Reporting (if using Sentry)
SENTRY_DSN=

# =============================================================================
# Development Tools Configuration
# =============================================================================

# Enable development mode features
DEV_MODE=true

# Enable hot reload (if supported)
HOT_RELOAD=true

# Enable debug logging
DEBUG=false

# JWT
JWT_SECRET="test-secret-minimum-32-characters-long-for-testing"
