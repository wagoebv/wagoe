;; check:test-tags allowlist — BOU-166 COMPLETE.
;;
;; Every deftest in the repo now carries exactly one pyramid tag
;; (^:unit / ^:integration / ^:contract). The allowlist is empty: the gate
;; enforces the whole tree with no exemptions. If a new lib is added, either
;; tag its deftests from the start or add its path prefix here temporarily.
{:allow-untagged []}
