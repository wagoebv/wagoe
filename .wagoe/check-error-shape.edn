;; Errors whose shape predates ADR-036 (BOU-322) and this gate.
;;
;; Empty, and that is the point: it shipped with 81 findings on 2026-08-18 and
;; BOU-323 walked them down to zero in three steps — 58 untyped throws, the
;; user library's returns, and the string :type values that crossed the
;; external/email boundary.
;;
;; A new entry needs a :why and a :count. An entry that stops exempting
;; anything fails the build, so this file cannot quietly become a drawer again.

[]
