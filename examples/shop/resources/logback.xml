<?xml version="1.0" encoding="UTF-8"?>
<!--
  The level here holds only until the system starts: :wagoe/logging applies
  :level from resources/conf/<env>/config.edn to the root logger. Change it
  there. INFO covers what runs before, and tools that never start the system
  (-M:migrate, -M:user-cli).

  stderr, not stdout: the :mcp alias loads this file too, and stdout is its
  JSON-RPC channel.
-->
<configuration>
  <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
    <target>System.err</target>
    <encoder>
      <pattern>%d{HH:mm:ss.SSS} %-5level [%thread] %logger{36} - %msg%n</pattern>
    </encoder>
  </appender>

  <root level="INFO">
    <appender-ref ref="CONSOLE" />
  </root>
</configuration>
