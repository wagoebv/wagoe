# render.yaml — Render Blueprint for Boundary
# https://docs.render.com/blueprint-spec
#
# Deploy: connect your repo on render.com and select this blueprint

services:
  - type: web
    name: wagoe-app
    runtime: docker
    dockerfilePath: resources/conf/dev/Dockerfile
    region: frankfurt
    plan: standard
    healthCheckPath: /health/ready
    envVars:
      - key: WAG_ENV
        value: prod
      - key: HTTP_PORT
        value: "3000"
      - key: JWT_SECRET
        generateValue: true
      - key: POSTGRES_HOST
        fromDatabase:
          name: wagoe-db
          property: host
      - key: POSTGRES_PORT
        fromDatabase:
          name: wagoe-db
          property: port
      - key: POSTGRES_DB
        fromDatabase:
          name: wagoe-db
          property: database
      - key: POSTGRES_USER
        fromDatabase:
          name: wagoe-db
          property: user
      - key: POSTGRES_PASSWORD
        fromDatabase:
          name: wagoe-db
          property: password

databases:
  - name: wagoe-db
    plan: standard
    region: frankfurt
    postgresMajorVersion: "16"
